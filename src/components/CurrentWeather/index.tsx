import React from "react";

/**
 * Displays graph with current weather on selected location; Data fetched from https://www.weatherbit.io/ API.
 */
const CurrentWeather: React.FC = () => <div>Current Weather</div>;

export default CurrentWeather;
