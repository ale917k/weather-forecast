import React from "react";

/**
 * Basic fallback page when no page is found on selected route.
 */
const PageNotFound: React.FC = () => <span>404 | Page Not Found</span>;

export default PageNotFound;
