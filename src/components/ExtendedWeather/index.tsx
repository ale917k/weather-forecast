import React from "react";

/**
 * Displays graph with extended 16-day weather on selected location; Data fetched from https://www.weatherbit.io/ API.
 */
const ExtendedWeather: React.FC = () => <div>16-day Weather</div>;

export default ExtendedWeather;
